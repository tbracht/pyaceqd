from setuptools import setup

setup(
      name='pyaceqd',
      version='0.0.1',
      install_requires=[
                        'numpy',
                        'matplotlib',
                        'tqdm',
                        'scipy'
                        ]
      )
